class billete {
  // CREAMOS LA CLASE
  constructor(valor, cantidad) {
    // SE CREA LA FUNCIÓN CONSTRUCTOR, QUE ES LA ENCARGADA DE CONSTRUIR. LE PASAMOS LOS VALORES
    this.valor = valor;
    this.cantidad = cantidad;
  }
}
